import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF0B0D0F);
  static const accent = Color(0xFF4FD8EB);
  static const textGrey = Color(0xFF909499);
  static const white = Colors.white;
}
